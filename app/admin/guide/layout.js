import GuideLayout from '../../../components/GuideLayout';

export const metadata = { title: "Guide — OK LET'S GO Admin" };

export default function Layout({ children }) {
  return <GuideLayout>{children}</GuideLayout>;
}
